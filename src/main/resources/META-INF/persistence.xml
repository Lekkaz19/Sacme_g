<?xml version="1.0" encoding="UTF-8"?>
<persistence version="3.0" xmlns="https://jakarta.ee/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="https://jakarta.ee/xml/ns/persistence https://jakarta.ee/xml/ns/persistence/persistence_3_0.xsd">

    <persistence-unit name="bdipa" transaction-type="JTA">
        <jta-data-source>jdbc/__default</jta-data-source> <!-- Usar el pool de conexiones por defecto del servidor -->
        <properties>
            <!-- No se necesitan propiedades JDBC aqui, las provee el JTA data source -->
            <!-- property name="jboss.as.jpa.managed" value="false"/ -->

            <!-- NO drop-and-create, usar la BD existente -->
            <property name="jakarta.persistence.schema-generation.database.action" value="none"/>
            
            <property name="hibernate.dialect" value="org.hibernate.dialect.MySQLDialect"/>
            <property name="hibernate.show_sql" value="true"/>
            <property name="hibernate.format_sql" value="true"/>
        </properties>
    </persistence-unit>
</persistence>
